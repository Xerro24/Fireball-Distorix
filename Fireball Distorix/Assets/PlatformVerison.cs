using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlatformVerison : MonoBehaviour
{

    public static bool Moblie;
    public bool ChangeMoblie;

    // Start is called before the first frame update
    void Start()
    {
        Moblie = ChangeMoblie;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
